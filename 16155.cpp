#include <bits/stdc++.h>
using namespace std;
string s,na[101];
vector<string> arr[101]; 
int N, M,t;
int main(){
    cin>>N>>M;
    for(int i =0; i <N ;i++){
        cin>>s;
        na[i]=s;
        arr[i].push_back(s);
    }
}
